<UserControl x:Class="UltimateChartist.UserControls.GraphUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UltimateChartist.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Name="MainGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="AntiqueWhite" BorderThickness="1">
            <Button x:Name="AddPanel" Content="Add Panel" Click="AddPanel_Click" HorizontalAlignment="Center"/>
        </Border>
        <Border Grid.Row="1" Background="AntiqueWhite" BorderThickness="1">
            <TextBox Text="Test" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBox>
        </Border>
    </Grid>
</UserControl>
