<UserControl x:Class="StockAnalyzerApp.CustomControl.StatisticsDlg.StatisticsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <ComboBox SelectedItem="{Binding IndicatorType}"  ItemsSource="{Binding IndicatorTypes}"  Width="60"/>
            <TextBox Text="{Binding Indicator}" Width="200" Margin="0,1"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label Content="Event" Width="60"/>
            <ComboBox SelectedItem="{Binding Event}" ItemsSource="{Binding Events}" Width="200" Margin="0,1"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Results}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Button Content="Calculate" Click="ButtonBase_OnClick"/>
        </StackPanel>
    </Grid>
</UserControl>
